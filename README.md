# Financial Insight LLM (ยังแค่ของ Salary after tax นะะตอนนี้)

## 🚀 Features

วิเคราะห์รายจ่าย/รายได้สุทธิต่อเดือน
-> คำนวณ Needs/Wants/Savings % (50/30/20 rules), DSR, Survival Ratio, Health Score
ตรวจจับหมวด culprit ที่ทำให้ค่าใช้จ่ายสูง,คำนวณเงินที่เกินกรอบมาตรฐานแบบ 50/30/20

LLM(Typhoon) สร้างข้อความสรุปให้ 3 ส่วน

left_panel → จุดอ่อนหลัก (หมวดใช้เยอะ / เกินเกณฑ์)

middle_panel → ความแข็งแรงทางการเงินรวมทั้งเดือน

right_panel → คำแนะนำที่ทำได้จริง 1–2 ข้อ

-> อันนี้ข้อความแสดงตามตำแหน่งที่จะแสดงผลบน [Dashboard]

---------------------------------------------------------------

## 📁 Project Structure
Financial_insight_salary_after_tax/

│

├── llm_client.py          # เชื่อม Typhoon + System/User Prompt

├── nic_analyzer.py        # วิเคราะห์ตัวเลขทั้งหมด + การคำนวณ score

├── playground.py          # ตัวอย่างการใช้งาน เล่น ๆๆๆ

├── requirements.txt       

----------------------------------------------------------------

## 🔧 Installation

### 1) (recommend) pre Virtual Environment
   
  python -m venv venv
  
venv\Scripts\activate      # Windows

source venv/bin/activate   # Mac/Linux

### 2) pip install -r requirements.txt
   
### 3) Set up Environment Variable (LLM)
* Key ได้จากในเว็บ Typhoon เข้าแล้ว -> กด Try now -> มันจะเป็น Typhoon playground มุมขวาบนจะมี API key กดแล้วคักลอกมาเรยย

เข้า Windows PowerShell:

setx TYPHOON_API_KEY "YOUR_KEY"


macOS / Linux:

export TYPHOON_API_KEY="YOUR_KEY"
* สามารถเช็คได้ว่า key โดนบันทึกจริงมั้ย โดยการปิด-เปิด window powershell แล้วพิม echo $Env:TYPHOON_API_KEY
* มันจะขึ้นประมาณ sk-xxxxxxxxxxxxxxxxxxxxxxxx 

----------------------------------------------------------------

## 📥 Input


| Key                | Meaning               |
|--------------------|---------------------------|
| net_income_monthly | รายได้สุทธิ               |
| needs_food         | ค่าอาหาร                  |
| needs_housing      | ค่าที่พัก                  |
| needs_transport    | ค่าเดินทาง                |
| needs_utilities    | ค่าสาธารณูปโภค            |
| needs_insurance    | ค่าเบี้ยประกัน            |
| needs_debt         | หนี้สิน                   |
| wants_misc         | ค่าใช้จ่ายเบ็ดเตล็ด        |


📤 Example Usage (ใน playground.py ก็มีให้เล่นน)
-------------------
```python
from nic_analyzer import generate_dashboard_data
from pprint import pprint
params = {
   
   "net_income_monthly": 30000,
    "needs_food": 5000,
    "needs_housing": 7000,
    "needs_transport": 2000,
    "needs_utilities": 1500,
    "needs_insurance": 800,
    "needs_debt": 3000,
    "wants_misc": 5000,
}

result = generate_dashboard_data(params)

pprint(result)
```
---------------------------------------------------------------

## 🧾 Output Example


ผลลัพธ์จะมี 2 ส่วน:

1. numbers → ตัวเลขทางการเงิน
นี่คือผลลัพธ์ตัวอย่าง:

```json
{
  "health_score": 36.0,
  "actual_needs_pct": 103.0,
  "actual_wants_pct": 20.0,
  "actual_savings_pct": -23.0,
  "dsr_pct": 15.0,
  "dsr_status": "Excellent",
  "survival_ratio": 0.82,
  "culprit_item": "housing",
  "culprit_amount": 7000
}
```



2. panels → ข้อความสรุป 3 ช่องจาก LLM

```json
{
  "left_panel": "ค่าใช้จ่ายจำเป็นอยู่ที่ 103% เกินเกณฑ์ 50% โดยหมวดที่อยู่อาศัยใช้ 7000 บาท เป็นส่วนที่สูงที่สุด",

  "middle_panel": "การเงินอยู่ในระดับวิกฤต เพราะใช้จ่ายเกินรายได้และไม่มีเงินออม",
  
  "right_panel": "ลองลดค่าเช่าหรือหาที่อยู่อาศัยที่ประหยัดขึ้น เพื่อให้เริ่มมีเงินเหลือเก็บ"
}
```


-----------------------------------------------------------------


## 🧠 How It Works

1️⃣ วิเคราะห์ตัวเลขรายเดือน

ผ่านคลาส FinancialHealthAnalyzer

สัดส่วน Needs/Wants/Savings

หนี้ (DSR)

Survival Ratio

คำนวณ Health Score

ตรวจจุด overspent

หา culprit category

2️⃣ รวมผลลัพธ์เป็น prompt ส่งเข้า LLM

ไฟล์ llm_client.py จะใส่ตัวเลขเข้า USER_PROMPT_TEMPLATE แล้วส่งให้ Typhoon

3️⃣ LLM สร้างผลลัพธ์ในรูปแบบ JSON

{
  "left_panel": "...",
  "middle_panel": "...",
  "right_panel": "..."
}

4️⃣ Backend/Frontend เรียกได้เรยย 👇

from nic_analyzer import generate_dashboard_data

result = generate_dashboard_data(params)


